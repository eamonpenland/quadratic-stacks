#!/usr/bin/env node

(() => {
  console.log("hello");

  //   (match-update (- (to-int (pow sum-sqrt u2)) (to-int sum-donations)))
  const sumMatch = 49342;
  const sumSqurt = 250;

  const match = sumSqurt ** 2 - sumMatch;
  console.log(match);
})();
